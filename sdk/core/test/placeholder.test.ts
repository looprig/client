// Placeholder test so `vitest run` has something to execute against the empty
// scaffold. Tasks 15/16 add real suites (contract.test.ts, transport.test.ts,
// etc.) alongside this file; remove this placeholder once real coverage exists.
import { describe, expect, it } from "vitest";

describe("sdk/core scaffold", () => {
  it("placeholder passes until real tests land", () => {
    expect(true).toBe(true);
  });
});
