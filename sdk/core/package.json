{
  "name": "@looprig/client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "json-schema-to-ts": "^3.1.1"
  },
  "devDependencies": {
    "typescript": "^6.0.0",
    "vitest": "^4.1.10"
  }
}
